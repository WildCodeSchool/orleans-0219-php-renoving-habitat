{% extends 'base.html.twig' %}

{% block title %}GoodDeal{% endblock %}

{% block body %}
    <h1>GoodDeal</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ good_deal.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ good_deal.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ good_deal.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('good_deal_index') }}">back to list</a>

    <a href="{{ path('good_deal_edit', {'id': good_deal.id}) }}">edit</a>

    {{ include('good_deal/_delete_form.html.twig') }}
{% endblock %}
