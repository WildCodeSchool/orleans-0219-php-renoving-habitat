{% extends 'base.html.twig' %}

{% block title %}GoodDeal index{% endblock %}

{% block body %}
    <h1>GoodDeal index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for good_deal in good_deals %}
            <tr>
                <td>{{ good_deal.id }}</td>
                <td>{{ good_deal.title }}</td>
                <td>{{ good_deal.description }}</td>
                <td>
                    <a href="{{ path('good_deal_show', {'id': good_deal.id}) }}">show</a>
                    <a href="{{ path('good_deal_edit', {'id': good_deal.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('good_deal_new') }}">Create new</a>
{% endblock %}
